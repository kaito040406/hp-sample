import { Typography } from "@material-ui/core"

interface FeaturesProps {
  title: string
  description: string
}

const NewTest = ({ title, description }: FeaturesProps) => {

  return (
    <>
    </>
  )
}

export default NewTest